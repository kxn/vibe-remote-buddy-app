# 操作反馈审计（2026-09-22）

- 固件更新：原先误用限高底部 Feedback，进度和取消按钮被裁切。改为独立顶层居中操作弹窗，离开设置页也持续显示；取消能力仍按更新阶段决定，完成后关闭进度并通知结果。
- 接收器选择：持久表单不应放通知区。改为选择弹窗，允许关闭，通过顶部接收器入口重新打开；连接失败在选择界面内显示。
- 机型库检查更新、同步、恢复默认：原先只有禁用控件。统一操作弹窗，不显示虚构百分比，结束后保留原来的结果通知。
- 按键确认：准备阶段进度移到现有固定状态位置，取消重复底部进度通知。
- 普通消息：不再使用 5rem 滚动容器。两行摘要，溢出时可打开完整详情，原有 8 秒自动关闭保留；查看详情时不会中途关闭。
- 首次安装、语音验证：已有专用步骤和状态区，保留原位状态，不另开重复进度。
- 绑定添加、删除、名称和按键保存、默认配置编辑、备份恢复：已有流程弹窗、禁用及保存状态；结果仍为短通知。没有将其转换为后台进度。

验证：159 项 Vitest、TypeScript/Vite 构建通过；既有真实 React 页面检查覆盖 1000×780、720×640、400×620；新增共享操作弹窗/长错误详情检查覆盖 1000×780、400×620 和 1.5 倍设备像素比，验证居中、取消按钮、Esc 不打断更新、滚轮隔离和完整长错误。均使用模拟传输，没有触发真实接收器更新。
